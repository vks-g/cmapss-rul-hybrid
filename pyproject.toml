[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rul"
version = "0.1.0"
description = "Remaining Useful Life prediction on NASA C-MAPSS: engineered-feature ML vs sequence DL"
readme = "README.md"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
